#!/bin/bash -e

# If running the rails server then create or migrate existing database
if [ "${1}" == "bash" ] && [ "${2}" == "-c" ] && [[ "${3}" == *"bundle exec rails server"* ]]; then
  bundle exec rails db:create
  bundle exec rails db:migrate
fi

exec "${@}"
